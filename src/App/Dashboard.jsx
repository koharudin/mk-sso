import { Card } from 'react-bootstrap';

export default (props) => {
  return (
    <Card>
      <Card.Header>
        <Card.Title as="h5">Dashboard</Card.Title>
      </Card.Header>
      <Card.Body>
        
      </Card.Body>
    </Card>
  );
};
