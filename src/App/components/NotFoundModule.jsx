import { Card } from 'react-bootstrap';

export default (props) => {
  return (
    <Card>
      <Card.Header>
        <Card.Title as={'h5'}>PERHATIAN</Card.Title>
      </Card.Header>
      <Card.Body>MODULE TIDAK DITEMUKAN</Card.Body>
    </Card>
  );
};
